apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: join-resources
spec:
  selector:
    app: join-resources
  type: LoadBalencer
  ports:
    - name: backend
      protocol: TCP
      port: 8080
      targetPort: 8080
