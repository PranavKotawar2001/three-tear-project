apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: join-resources
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: join-resources
